<UserControl x:Class="TryOut.Views.MainPages.DisplayControl.DisplayAsyncView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:NavigationPresenter="clr-namespace:TryOut.ViewModels.MainPages.NavigationPresenter"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance NavigationPresenter:NavigationAsyncViewModel}"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Grid>
        <StackPanel>
            <TextBlock Text="DisplayAsyncView" />
            <TextBlock Text="{Binding Status}" Margin="0,20,0,0" />
        </StackPanel>
    </Grid>

</UserControl>